syntax = "proto2";

package apollo_perception_standalone;

import "proto/can_card_parameter.proto";

message SensorCanbusConf {
  optional apollo_perception_standalone.CANCardParameter can_card_parameter = 1;
  optional bool enable_debug_mode = 2 [default = false];
  optional bool enable_receiver_log = 3 [default = false];
}
